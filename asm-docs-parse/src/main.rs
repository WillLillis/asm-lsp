// TODO: Write a simple CLI tool to take in the project's XML files,
// serialize them, and write them to a file accessible by the main
// server file
// Will probably want to move existing tests over to this crate, and then write
// some new tests to make sure we can properly deserialize the converted docs
// in the main server
fn main() {
    println!("Hello, world!");
}
